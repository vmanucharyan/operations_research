package nwcorner

import org.scalatest.FunSuite

import org.junit.runner.RunWith
import org.scalatest.junit.JUnitRunner

import nwcorner.NWCorner

@RunWith(classOf[JUnitRunner])
class NWCornerSuite extends FunSuite {

  test("initialResult") {
    assert(5 === 5)
  }
}